<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/planapp?useSSL=false</property>
        <property name="connection.username">planapp</property>
        <property name="connection.password">planapp</property>

        <!-- JDBC connection pool settings -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>

        <!-- The mapping information of entities -->
        <mapping class="models.Gebruiker"/>
        <mapping class="models.PlanningCard"/>
        <!--  <mapping class="models.Example"/>  -->





    </session-factory>
</hibernate-configuration>